# NNNN. <Short decision title>

**Status:** proposed | accepted | rejected | superseded by ADR-NNNN
**Date:** YYYY-MM-DD
**TZ section(s):** §X.Y

## Context

What question forced this decision? What was investigated, and what's the constraint (device
limitation, plugin API reality, licensing, etc.)? Say plainly what was verified in this environment
vs. what still needs a real device/emulator — don't blur the two.

## Decision

The concrete choice made, stated as an instruction a future agent can follow without re-reading the
investigation (e.g. "use `llama-cpp-capacitor` 0.1.5; call `initLlama({...})`, not
`init({...})` — the README is wrong about the method name").

## Consequences

- What this unblocks in `ROADMAP.md`.
- What fallback exists if this decision turns out to be wrong later, and what would trigger
  reconsidering it.
- Any `docs/decisions.md` row this resolves.
