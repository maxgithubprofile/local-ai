# minimal-capacitor-app (Phase 7 placeholder)

Example Capacitor app exercising `local-ai` end to end — TZ §12, ROADMAP.md Phase 7. Not scaffolded
yet; deferred until the public API in `src/core/client/local-ai-client.ts` is actually implemented
(Phases 1-6), so the example doesn't have to be rewritten as the API firms up.

Planned scope per the TZ: 2+ chats, one running in Mode B (`upsertChat`/`appendMessages`, §9.6), an
embedding update independent of the model update (§5.6), and a screen for "device not
supported/not eligible" (§6).
